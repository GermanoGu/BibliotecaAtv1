@model Usuario
@{
    ViewData["Title"] = "Atualizar Usuarios";
}

<div class="row justify-content-md-center">
    <form asp-action="AtualizaUser" method="POST" class="col-md-4">
        <input type="hidden" asp-for="Id" />
        <div class="form-group">
            <label asp-for="Nome">Nome</label>
            <input asp-for="Nome" class="form-control" required/>
        </div>
        <div class="form-group">
            <label asp-for="Login">Login</label>
            <input asp-for="Login" class="form-control" required/>
        </div>
        <div>
            <label asp-for="Senha">Senha</label>
            <input asp-for="Senha"class="form-control"required />
        </div>
         <div class="form-group">
            <select asp-for="Tipo" required>
            <option value="@Usuario.ADMIN">Administrador</option>
            <option value="@Usuario.PADRAO">Padrão</option>
            </select>
        </div>
        <input type="submit" value="Editar" class="btn btn-primary"required/>
    </form>
</div>